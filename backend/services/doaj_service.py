from typing import List, Dict, Any

class DOAJService:

    @staticmethod
    def search_papers(query: str, max_results: int = 10) -> List[Dict[str, Any]]:
        # DOAJ API unstable — disabled for stability
        return []
